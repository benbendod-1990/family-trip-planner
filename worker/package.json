{
  "name": "family-trip-planner-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker — AI proxy (Claude API) for family-trip-planner",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "~5.6.0",
    "wrangler": "^4.0.0"
  }
}
